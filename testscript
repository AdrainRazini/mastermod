local TweenService = game:GetService("TweenService")

-- Criação do GUI
local ScreenGui = Instance.new("ScreenGui")
local ModMenu = Instance.new("Frame")
local TitleBar = Instance.new("Frame")
local MinimizeButton = Instance.new("TextButton")
local Content = Instance.new("ScrollingFrame")
local ConfigButton = Instance.new("TextButton")
local ConfigFrame = Instance.new("Frame")
local Buttons = {}

-- Propriedades do GUI
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Configuração do menu principal
ModMenu.Name = "ModMenu"
ModMenu.Parent = ScreenGui
ModMenu.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
ModMenu.Size = UDim2.new(0, 200, 0, 300)
ModMenu.Position = UDim2.new(0.1, 0, 0.1, 0)
ModMenu.Active = true
ModMenu.Draggable = true

-- Barra de título
TitleBar.Name = "TitleBar"
TitleBar.Parent = ModMenu
TitleBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TitleBar.Size = UDim2.new(1, 0, 0, 30)

local Title = Instance.new("TextLabel", TitleBar)
Title.Text = "MODMASTER"
Title.Size = UDim2.new(1, -30, 1, 0)
Title.BackgroundTransparency = 1
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextScaled = true

-- Botão de minimizar
MinimizeButton.Name = "MinimizeButton"
MinimizeButton.Parent = TitleBar
MinimizeButton.Text = "-"
MinimizeButton.Size = UDim2.new(0, 30, 1, 0)
MinimizeButton.Position = UDim2.new(1, -30, 0, 0)
MinimizeButton.TextColor3 = Color3.new(1, 1, 1)
MinimizeButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)

-- Conteúdo do menu (ScrollingFrame)
Content.Name = "Content"
Content.Parent = ModMenu
Content.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
Content.Size = UDim2.new(1, 0, 1, -30)
Content.Position = UDim2.new(0, 0, 0, 30)
Content.CanvasSize = UDim2.new(0, 0, 1, 0)
Content.ScrollBarThickness = 8

-- Botão de Configurações
ConfigButton.Name = "ConfigButton"
ConfigButton.Parent = TitleBar
ConfigButton.Text = "⚙️"
ConfigButton.Size = UDim2.new(0, 30, 1, 0)
ConfigButton.Position = UDim2.new(0, 0, 0, 0)
ConfigButton.TextColor3 = Color3.new(1, 1, 1)
ConfigButton.BackgroundColor3 = Color3.fromRGB(0, 0, 200)

-- Janela de configurações
ConfigFrame.Name = "ConfigFrame"
ConfigFrame.Parent = ScreenGui
ConfigFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
ConfigFrame.Size = UDim2.new(0, 150, 0, 200)
ConfigFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
ConfigFrame.Visible = false

local ColorPicker = Instance.new("TextBox", ConfigFrame)
ColorPicker.PlaceholderText = "Cor RGB (60,60,60)"
ColorPicker.Size = UDim2.new(1, -10, 0, 30)
ColorPicker.Position = UDim2.new(0, 5, 0, 5)
ColorPicker.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
ColorPicker.TextColor3 = Color3.new(0, 0, 0)

local SizeSlider = Instance.new("TextBox", ConfigFrame)
SizeSlider.PlaceholderText = "Tamanho (ex: 200)"
SizeSlider.Size = UDim2.new(1, -10, 0, 30)
SizeSlider.Position = UDim2.new(0, 5, 0, 40)
SizeSlider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
SizeSlider.TextColor3 = Color3.new(0, 0, 0)

local ApplyButton = Instance.new("TextButton", ConfigFrame)
ApplyButton.Text = "Aplicar"
ApplyButton.Size = UDim2.new(1, -10, 0, 30)
ApplyButton.Position = UDim2.new(0, 5, 0, 75)
ApplyButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
ApplyButton.TextColor3 = Color3.new(1, 1, 1)

-- Variável para saber o estado de minimização
local isMinimized = false

-- Função para abrir/fechar configurações
ConfigButton.MouseButton1Click:Connect(function()
    ConfigFrame.Visible = not ConfigFrame.Visible
end)

-- Função de aplicação de configurações
ApplyButton.MouseButton1Click:Connect(function()
    local colorValues = ColorPicker.Text:match("(%d+),(%d+),(%d+)")
    if colorValues then
        local r, g, b = colorValues:match("(%d+),(%d+),(%d+)")
        ModMenu.BackgroundColor3 = Color3.fromRGB(tonumber(r), tonumber(g), tonumber(b))
    end
    local sizeValue = tonumber(SizeSlider.Text)
    if sizeValue then
        ModMenu.Size = UDim2.new(0, sizeValue, 0, 300)
    end
end)

-- Créditos
local Credits = Instance.new("TextLabel", ModMenu)
Credits.Text = "Criado por: adrian75556435"
Credits.Size = UDim2.new(1, 0, 0, 20)
Credits.Position = UDim2.new(0, 0, 1, -20)
Credits.BackgroundTransparency = 1
Credits.TextColor3 = Color3.new(1, 1, 1)
Credits.TextScaled = true

-- Função para minimizar/maximizar com animação
local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
MinimizeButton.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    local sizeTween, titleTween

    if isMinimized then
        -- Minimizar o menu
        sizeTween = TweenService:Create(ModMenu, tweenInfo, {Size = UDim2.new(0, 50, 0, 50)})
        titleTween = TweenService:Create(Title, tweenInfo, {TextTransparency = 1})
        Content.Visible = false
    else
        -- Restaurar o menu
        sizeTween = TweenService:Create(ModMenu, tweenInfo, {Size = UDim2.new(0, 200, 0, 300)})
        titleTween = TweenService:Create(Title, tweenInfo, {TextTransparency = 0})
        Content.Visible = true
    end

    sizeTween:Play()
    titleTween:Play()
end)

-- Criando um botão de exemplo
createButton("INVENTÁRIO", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/AdrainRazini/mastermod/refs/heads/main/inventario.lua"))()
end)
